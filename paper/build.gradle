plugins {
    id 'java'
}

project.version = gradle.ext.hbVersion

// Version-specific Paper configuration lives under paper/versions/*.
// Keep the root project as a thin wrapper so the active target is the only one
// applying Paperweight and resolving a dev bundle.
