plugins {
    id "java"
}

// Version is defined per-version via Stonecutter in the versioned subprojects

repositories {
    mavenCentral()
}

// Version-specific Fabric configuration lives under fabric/versions/*.
// Keep the root project as a thin wrapper so the active target is the only one
// applying Loom and resolving Minecraft mappings.
